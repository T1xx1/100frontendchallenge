<div class="bg bg-gray-100 bg-opacity-50 rounded-xl px-14 py-24 w-full">
   <div class="w-2/3 space-y-5 font-medium">
      <div>
         <div class="text-sm text-gray-400 font-bold">Join us</div>
         <h2 class="text-3xl font-bold">Create your new account</h2>
      </div>

      <form class="space-y-3">
         <fieldset class="bg-blue-100 bg-opacity-50 rounded-lg px-2 py-1">
            <div class="text-xs text-gray-400">Username</div>

            <input
               type="text"
               class="bg-transparent text-sm font-semibold w-full outline-none"
            />
         </fieldset>

         <fieldset class="bg-blue-100 bg-opacity-50 rounded-lg px-2 py-1">
            <div class="text-xs text-gray-400">Email</div>

            <input
               type="email"
               class="bg-transparent text-sm font-semibold w-full outline-none"
            />
         </fieldset>

         <fieldset class="bg-blue-100 bg-opacity-50 rounded-lg px-2 py-1">
            <div class="text-xs text-gray-400">Password</div>

            <input
               type="password"
               class="bg-transparent text-sm font-semibold w-full outline-none"
            />
         </fieldset>

         <button
            class="bg-blue-600 hover:bg-blue-500 rounded-full py-2 w-full text-white text-sm shadow-md shadow-blue-500"
            >Create account</button
         >
      </form>

      <div class="text-xs text-center text-gray-400">
         Already have an account? <a
            href=""
            class="text-blue-700 hover:underline">Sign in</a
         >
      </div>
   </div>
</div>

<style lang="scss">
   .bg {
      background-image: url("./bg.png");
   }
</style>
